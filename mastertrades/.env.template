# Local secrets template. Copy this file to `.env` (same folder) and fill in
# real values. The `.env` file is gitignored and will never be committed.
#
# To activate, copy:
#   Copy-Item .env.template .env
# Then edit .env in any editor and paste your key after the `=`.

POLYGON_API_KEY=
